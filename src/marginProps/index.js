const marginProps = ({
  marginTop = 0,
  marginRight = 0,
  marginBottom = 0,
  marginLeft = 0,
}) => `margin: ${marginTop} ${marginRight} ${marginBottom} ${marginLeft};`;

export default marginProps;
